# Настройка окружений — AI Meal Planner

## 📌 Цель

Документ описывает конфигурацию окружений (Dev, Staging, Prod), управление переменными окружения, стратегию ротации секретов и планы резервного копирования/восстановления для проекта **AI Meal Planner**.

---

## ⚙️ Окружения

### 🚧 Dev (локальная разработка)

- Локальный запуск backend: `uvicorn app.main:app --reload` (FastAPI + Python)
- База данных: локальный **SQLite**
- LLM: собственная fine-tuning LLM
- Мобильное приложение: Android Studio
- База продуктов: локальная SQLite

Используется для разработки, отладки и локального тестирования AI-генерации меню.

---

### 🧪 Staging (предпрод)

- Развёртывание на тестовом сервере (Docker Compose)
- База данных: отдельный SQLite (тестовый инстанс)
- LLM: fine-tuning LLM 
- Мобильное приложение: test builds через Firebase App Distribution
- API продуктовых данных: тестовый инстанс
- Используется для интеграционных тестов, проверки AI-генерации и UX-тестирования

---

### 🚀 Prod (продакшен)

- Развёртывание на выделенном сервере или Kubernetes-кластере
- SQLite — боевой инстанс с репликацией и регулярными бэкапами
- LLM: собственная модель
- Мобильное приложение: публикация в Google Play Store
- Все .env переменные хранятся в секрет-менеджере
- Мониторинг производительности AI-генерации
- CDN для статических ресурсов (изображения рецептов)

---

## 🔑 Environment Variables

Ниже перечислены ключевые переменные окружения, используемые в проекте.

### Пример `.env` (структура):

```env
# Backend Server
SERVER_URL=
API_PREFIX=/api/v1
ENVIRONMENT=development

# Database
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
POSTGRES_HOST=
POSTGRES_PORT=
DATABASE_URL=

# LLM API (DeepSeek/OpenAI)
LLM_API_KEY=
LLM_API_BASE_URL=
LLM_MODEL_NAME=deepseek-chat
LLM_MAX_TOKENS=4000
LLM_TEMPERATURE=0.7

# Product Database
PRODUCTS_API_URL=
PRODUCTS_API_KEY=

# Mobile App
MOBILE_APP_URL=
ANDROID_PACKAGE_NAME=

# CORS
CORS_ORIGINS=["http://localhost:3000", "http://localhost:8080"]

# Rate Limiting
RATE_LIMIT_PER_MINUTE=60

# Meal Generation
MAX_GENERATION_TIME=45
DEFAULT_BUDGET=3000
```

## 🧩 Политика окружений

### **Dev**

- Локальные базы данных без репликации
- Упрощённая безопасность для отладки
- Прямой доступ к .env разрешён разработчикам
- Отключены некоторые проверки для ускорения разработки

### **Staging**

- Ограниченные права доступа к базам данных
- .env хранится в зашифрованном виде в репозитории
- Включены все проверки безопасности
- Мониторинг качества AI-генерации

### **Prod**

- Все ключи — строго через Secret Manager:
  - Github secrects
- Высоконагруженная SQLite с репликацией
- Строгие лимиты запросов и мониторинг
- Полная изоляция сервисов
- Прямой доступ к .env запрещён
- Регулярный аудит безопасности

---

# 🔒 Secrets Rotation

- Все чувствительные данные:
  - Database пароли
  — хранятся в **Secret Storage**.

### Политика ротации:

- **Database пароли**: ротация каждые **180 дней**
- Прямой доступ имеют только:
  - DevOps инженеры
  - Security team
  - CI/CD Pipeline

---

# 💾 Backup и Recovery

## 📦 Backup (резервное копирование)


### Конфигурации

- Версионирование: `.env`, `docker-compose.yml`
- Хранение в Git с ограниченным доступом

### AI-модели и промпты

- **Версионирование промптов** для генерации меню
- **Бэкап конфигураций** AI-пайплайна
- **Логи генераций** для анализа качества

---

## ♻️ Recovery (восстановление после сбоя)

### Процедура восстановления:

1. **Приоритизация**: восстановление базы данных пользователей
2. **SQLite**: восстановление из последнего стабильного WAL + дамп
3. **Redis**: восстановление из RDB-снимка
4. **Конфигурации**: развёртывание из Git-репозитория
5. **Перезапуск сервисов** в порядке зависимостей:
   - SQLite
   - Backend API
   - Фоновые workers (генерация меню)
6. **Валидация состояния**: проверка всех API endpoints
7. **Уведомление пользователей**: о временных неудобствах

### 🔍 Smoke-тест после восстановления:

1. Регистрация нового тестового пользователя
2. Заполнение профиля (цель: похудение, бюджет: 2500₽)
3. Генерация недельного плана питания
4. Проверка корректности AI-генерации (КБЖУ, рецепты)
5. Формирование списка покупок
6. Сохранение плана в историю
7. Отправка тестового email с меню
8. Проверка push-уведомления в мобильном приложении
9. Валидация оффлайн-доступа к сохранённым планам

### Время восстановления (RTO):

- **Критичные данные (SQLite)**: ≤ 1 часа
- **Полное восстановление системы**: ≤ 4 часов

---

# 📂 Итог

Документ определяет:

- Полную структуру **Dev / Staging / Prod** окружений для AI Meal Planner
- Детальную политику управления переменными окружения с акцентом на AI-интеграции
- Механизм ротации секретов с различными циклами для разных типов ключей
- Комплексные процедуры резервного копирования с учётом специфики данных о питании
- Пошаговый план восстановления с smoke-тестами для проверки AI-функционала

**Особенности для AI Meal Planner:**
- Приоритет на сохранность пользовательских планов питания
- Отдельное внимание к бэкапу конфигураций AI-генерации
- Мониторинг качества восстановленных AI-функций
- Гарантия консистентности данных между пищевой БД и пользовательскими планами
